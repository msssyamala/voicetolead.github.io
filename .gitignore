# Ignore macOS system files
.DS_Store

# Ignore Windows system files
Thumbs.db

# Node modules (if using build tools like Tailwind, npm, etc.)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Ignore package locks (optional, if you're not committing them)
package-lock.json
yarn.lock

# Build output (if using a generator like Jekyll/Hugo/Vite/etc.)
dist/
build/
public/
_out/
.cache/

# Environment files (never commit secrets)
.env
.env.local
.env.*.local

# Logs and temporary files
*.log
*.tmp
*.swp

# Editor configs
.vscode/
.idea/

# GitHub Pages source folder if separate from build
_site/

# If using Jekyll locally
.sass-cache/
.jekyll-cache/
